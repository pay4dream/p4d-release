# Pay4Dream 1.0.4

1.0.4 聚焦创作工作台的界面整理和现有流程的可用性回归。

## 更新内容

- 统一视频、图片、3D 分镜、剪辑、任务和设置页面的视觉层级与控件样式；视频工作台采用镜头列表、主作品区和参数面板布局。
- 优化候选预览与采用流程、失败恢复、跨镜头状态刷新，以及窄窗口下的顶栏、剪辑片段和 3D 视图显示。
- 改进本地视频缩略图的静帧呈现；主播放器的既有行为保持不变。
- Public 构建中的 3D 角色使用程序化人偶表示，保留可见、可选和场景物体旋转等编辑交互。Public 构建不包含该类第三方模型资源，也不会请求其下载。

## 已验证范围

- 已完成本地前端回归、候选专项回归，以及隔离 macOS 原生应用的实际操作与保存后重开验证。
- Public 3D 验证确认：程序化人偶处于 fallback 状态、未发生对该类第三方完整骨骼角色模型资源的请求；通过 macOS 真实界面选中场景物体并拖动旋转 gizmo 后，物体旋转和场景世界姿态均发生变化。
- 本轮未进行新的付费媒体生成。

## 发布资产

macOS Public DMG 与 Windows Public EXE 仅在同版本校验、SHA-256 和字节数写入 `release-meta.json` 且 Release 资产完成读回后发布。本页当前是待 `public_ready` 的准备稿，不表示安装包已上传或已发布。
